@model EventManager.Models.Usuario

@{
    ViewData["Title"] = "Perfil";
}

@* <h1>Details</h1> *@

@* <div> *@
@*     <h4>Usuario</h4> *@
@*     <hr /> *@
@*     <dl class="row"> *@
@*         <dt class = "col-sm-2"> *@
@*             @Html.DisplayNameFor(model => model.Nome) *@
@*         </dt> *@
@*         <dd class = "col-sm-10"> *@
@*             @Html.DisplayFor(model => model.Nome) *@
@*         </dd> *@
@*         <dt class = "col-sm-2"> *@
@*             @Html.DisplayNameFor(model => model.Email) *@
@*         </dt> *@
@*         <dd class = "col-sm-10"> *@
@*             @Html.DisplayFor(model => model.Email) *@
@*         </dd> *@
@*         <dt class = "col-sm-2"> *@
@*             @* @Html.DisplayNameFor(model => model.SenhaHash) *@ 
@*         </dt> *@
@*         <dd class = "col-sm-10"> *@
@*             @Html.DisplayFor(model => model.SenhaHash) *@
@*         </dd> *@
@*     </dl> *@
@* </div> *@

@* <hr /> *@
@* <hr /> *@

@* <h3>Convidados</h3> *@

@* @if (Model.Eventos?.Any() == true) *@
@* { *@
@*     <ul> *@
@*         @foreach (var evento in Model.Eventos) *@
@*         { *@
@*             <li> *@
@*                 @evento.Titulo | <a asp-controller="Convidados" asp-action="Create" asp-route-eventoId="@evento.Id" class="btn btn-primary">Convidar evento</a> *@
@*                 <a type="button" class="btn btn-danger" asp-controller="Eventos" asp-action="Delete" asp-route-id="@evento.Id">Remover Evento</a> *@
@*                 <a type="button" class="btn btn-primary" asp-controller="Eventos" asp-action="Edit" asp-route-id="@evento.Id">Editar evento</a> *@
@*             </li> *@
@*         } *@
@*     </ul> *@
@* } *@
@* else *@
@* { *@
@*     <p>Sem Evento cadastrado.</p> *@
@* } *@
@* <div> *@
@*     <a asp-controller="Eventos" asp-action="Create" asp-route-usuarioId="@Model.Id" class="btn btn-primary">Adicionar Evento</a> *@
@*     <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> | *@
@*     <a asp-action="Index">Back to List</a> *@
@* </div> *@


<div class="container mt-5">
    <div class="row">
        <div class="col-md-4">
            <div class="text-center">
                <img src="https://via.placeholder.com/150" class="rounded-circle img-thumbnail" alt="Foto de perfil">
                <h2>@Model.Nome</h2>
                <p>@Model.Email</p>
                <a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model?.Id">Editar Perfil</a>
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@Model?.Id">Excluir Conta</a>
                <a class="btn btn-success" asp-controller="Eventos" asp-action="Create" asp-route-usuarioId="@Model.Id">Criar Novo Evento</a>
            </div>
        </div>

        <div class="col-md-8">
            <h2>Meus Eventos</h2>
            @if (Model.Eventos?.Any() == true){

                <table class="table">
                        <thead>
                            <tr>
                                <th>Título</th>
                                <th>Data</th>
                                <th>Hora</th>
                                <th>Ações</th>
                            </tr>
                        </thead>
                    @foreach (var evento in Model.Eventos)
                    {


                        <tbody>
                            <tr >
                                <td>@evento.Titulo</td>
                                <td>@evento.Data</td>
                                <td>@evento.Hora</td>
                                <td>
                                    <a class="btn btn-secondary" asp-controller="Convidados" asp-action="Create" asp-route-eventoId="@evento.Id">Convidar</a>
                                    <a class="btn btn-primary" asp-controller="Eventos" asp-action="Edit" asp-route-id="@evento.Id">Editar</a>
                                    <a class="btn btn-danger" asp-controller="Eventos" asp-action="Delete" asp-route-id="@evento.Id">Excluir</a>
                                </td>
                            </tr>
                        </tbody>
                    }
                </table>

            }else{
                <p>Sem Evento cadastrado.</p>
            }
        </div>
    </div>
</div>

